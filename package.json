{
  "name": "vitap-connect",
  "private": true,
  "version": "1.0.0",
  "description": "VITAP Connect full-stack Web Technology class project",
  "scripts": {
    "install:all": "npm.cmd --prefix frontend install && npm.cmd --prefix backend install",
    "dev": "concurrently \"npm.cmd --prefix backend run dev\" \"npm.cmd --prefix frontend run dev\"",
    "dev:frontend": "npm.cmd --prefix frontend run dev",
    "dev:backend": "npm.cmd --prefix backend run dev",
    "build": "npm.cmd --prefix frontend run build",
    "seed:admin": "npm.cmd --prefix backend run seed:admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

